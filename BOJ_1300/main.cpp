#include <iostream>
#include <vector>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int N, k;
    int i;
    vector<int> V;
    cin >> N >> k;
    for(i=0; k < i(i+1)/2 && i-1 < N; i++); // i-1 은 k를 넘어서기 직전
    // k를 넘어서기 직전부터 k까지 해야함
    if(i-1 >= N) {
        
    }

    return 0;
}
